{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "turbo run check",
    "db:generate": "cd packages/db && deno run -A npm:drizzle-kit@latest generate",
    "db:migrate": "cd packages/db && deno run -A npm:drizzle-kit@latest migrate",
    "db:push": "cd packages/db && deno run -A npm:drizzle-kit@latest push",
    "db:studio": "cd packages/db && deno run -A npm:drizzle-kit@latest studio --host=0.0.0.0 --port=5555",
    "db:seed": "deno run --allow-all packages/db/src/seed.ts"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "drizzle-kit": "^0.31.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "@std/testing": "jsr:@std/testing@^1.0.18",
    "@std/expect": "jsr:@std/expect@^1.0.19"
  },
  "dependencies": {
    "drizzle-orm": "^0.42.0",
    "nodemailer": "^8.0.6",
    "pino-pretty": "^13.1.3",
    "postgres": "^3.4.5"
  }
}
