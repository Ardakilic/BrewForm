{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "turbo run check"
  },
  "devDependencies": {
    "@types/nodemailer": "^8.0.0",
    "prisma": "^6.19.3",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "nodemailer": "^8.0.6",
    "pino-pretty": "^13.1.3"
  }
}
