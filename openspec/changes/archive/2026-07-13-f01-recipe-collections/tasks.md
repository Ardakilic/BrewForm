## 1. Database — Schema and Migration

- [x] 1.1 Add `collections` table to `packages/db/src/schema.ts` (singular `'collection'`, plural export `collections`): `id` varchar 36 + `crypto.randomUUID()`, `userId` FK to `users.id`, `name` varchar 255 notNull, `description` text nullable, `visibility` reuse `visibilityEnum` default `'private'`, `createdAt`/`updatedAt`/`deletedAt` timestamps. Table-config array: `collection_user_id_idx`, `collection_visibility_idx`, `collection_created_at_idx`, `collection_deleted_at_idx`. Include JSDoc docblock on the export.
- [x] 1.2 Add `collectionItems` table to `packages/db/src/schema.ts` (singular `'collection_item'`, plural export `collectionItems`): `id` varchar 36 + `crypto.randomUUID()`, `collectionId` FK with `onDelete: 'cascade'`, `recipeId` FK with no `onDelete`, `sortOrder` integer default 0, `createdAt` timestamp with `/** Audit timestamp — when the recipe was added to this collection. */` JSDoc. Table-config array: composite unique `collection_item_collection_id_recipe_id_unique` on `(collectionId, recipeId)`, `collection_item_collection_id_idx`, `collection_item_recipe_id_idx`. Include JSDoc docblock.
- [x] 1.3 Add `collectionsRelations` block: `user: one(users, { fields: [collections.userId], references: [users.id] })`, `items: many(collectionItems)`. Add JSDoc.
- [x] 1.4 Add `collectionItemsRelations` block: `collection: one(collections, { fields: [collectionItems.collectionId], references: [collections.id] })`, `recipe: one(recipes, { fields: [collectionItems.recipeId], references: [recipes.id] })`. Add JSDoc.
- [x] 1.5 Add `collections: many(collections)` to the existing `usersRelations` block. Add `collectionItems: many(collectionItems)` to the existing `recipesRelations` block. Both additive, with JSDoc on the new line.
- [x] 1.6 Run `make db-generate` to auto-generate migration `0009_<codename>.sql` (do NOT manually edit SQL — per AGENTS.md, manual edits break Drizzle's hash-based migration tracking)
- [x] 1.7 Run `make db-migrate` to apply the migration
- [x] 1.8 Add index assertions to `packages/db/src/schema-indexes.test.ts`: a new `describe('Collections table indexes')` block with `it` cases for each of the 4 collection indexes (3 expects each: name, snake_case columns, `isUnique: false`) and a new `describe('Collection items table indexes')` block with `it` cases for the 2 single-column indexes and the composite unique (assert `isUnique: true` for the unique constraint). Import `collections` and `collectionItems` in the import block.
- [x] 1.9 Add `createdAt` column assertions to `packages/db/src/schema-columns.test.ts`: a new `describe('collectionItems createdAt audit column')` block with three `it`s (column defined, `notNull`, default defined). Import `collectionItems` in the import block.

## 2. Shared — Input Schemas

- [x] 2.1 Create `packages/shared/src/schemas/collection.ts` with `CollectionCreateSchema`, `CollectionUpdateSchema`, `CollectionAddRecipeSchema`, `CollectionReorderSchema`, `CollectionListFilterSchema`, and inferred types `CollectionCreate`/`CollectionUpdate`. Use `z.enum(VISIBILITY_VALUES)` for visibility (import from `../constants/index.ts`). Use `z.uuid()`, `z.coerce.number().int().positive().default(...)` for pagination. Add JSDoc docblocks on every export.
- [x] 2.2 Create `packages/shared/src/schemas/collection_test.ts` with unit tests: `CollectionCreateSchema` (valid, missing name, defaults visibility to private, accepts all visibility values), `CollectionUpdateSchema` (all-optional), `CollectionAddRecipeSchema` (valid, rejects non-UUID recipeId), `CollectionReorderSchema` (valid, rejects empty array), `CollectionListFilterSchema` (defaults page=1, perPage=20).
- [x] 2.3 Add `export { CollectionCreateSchema, CollectionUpdateSchema, CollectionAddRecipeSchema, CollectionReorderSchema, CollectionListFilterSchema } from './collection.ts'` and `export type { CollectionCreate, CollectionUpdate } from './collection.ts'` blocks to `packages/shared/src/schemas/index.ts`.

## 3. Shared — Response Output Schemas

- [x] 3.1 Create `packages/shared/src/schemas/responses/collection.ts` with `CollectionOutputSchema`, `CollectionListItemOutputSchema` (extends with `recipeCount`), `CollectionItemOutputSchema` (with nested `RecipeListItemOutputSchema`), `CollectionDetailOutputSchema` (extends with `author: RecipeAuthorMiniSchema`, `items: array(CollectionItemOutputSchema)`, `recipeCount`). Import `RecipeListItemOutputSchema` from `./recipe.ts` and `RecipeAuthorMiniSchema` from `./_shared.ts`. Add `export type` for each. Add JSDoc docblocks.
- [x] 3.2 Create `packages/shared/src/schemas/responses/collection_test.ts` with unit tests using the `wire()` JSON round-trip helper: build realistic payloads (collection row, list item with recipeCount, detail with author + items + nested recipe), round-trip through `JSON.parse(JSON.stringify())`, assert `safeParse` success + `.toEqual(wired)`. Include rejection tests (missing recipeCount, missing author).
- [x] 3.3 Add `export { CollectionOutputSchema, CollectionListItemOutputSchema, CollectionItemOutputSchema, CollectionDetailOutputSchema } from './collection.ts'` to `packages/shared/src/schemas/responses/index.ts`.
- [x] 3.4 Add `export type { CollectionOutput, CollectionListItemOutput, CollectionItemOutput, CollectionDetailOutput } from './responses/collection.ts'` to `packages/shared/src/schemas/index.ts`.

## 4. API — Model Layer

- [x] 4.1 Create `apps/api/src/modules/collection/model.ts`. Add `// deno-lint-ignore-file no-explicit-any require-await` as line 1. Import `{ db } from '@brewform/db'`, `{ collections, collectionItems, recipes, users } from '@brewform/db/schema'`, operators `{ and, asc, count, desc, eq, inArray, isNull, sql } from 'drizzle-orm'`, `createLogger` from `../../utils/logger/index.ts`. Create `const modelLog = createLogger('collection-model')`.
- [x] 4.2 Implement `findById(id)` — `db.query.collections.findFirst({ where: and(eq(collections.id, id), isNull(collections.deletedAt)), with: { user: {...}, items: { with: { recipe: ... }, orderBy: asc(collectionItems.sortOrder) } } })`. Add JSDoc.
- [x] 4.3 Implement `findByUserId(userId, page, perPage, visibility?)` — `db.query.collections.findMany({ where: and(eq(collections.userId, userId), isNull(collections.deletedAt), visibility ? eq(collections.visibility, visibility) : undefined), orderBy: desc(collections.createdAt), limit: perPage, offset: (page-1)*perPage })` + parallel `count()` query. Compute `recipeCount` per collection via a subquery or batch count. Add JSDoc.
- [x] 4.4 Implement `findPublicByUserId(userId, page, perPage)` — same as `findByUserId` but with `eq(collections.visibility, 'public')` added to the WHERE. Add JSDoc.
- [x] 4.5 Implement `create(data)` — `db.insert(collections).values(data).returning()`. Add JSDoc.
- [x] 4.6 Implement `update(id, data)` — `db.update(collections).set({ ...data, updatedAt: new Date() }).where(and(eq(collections.id, id), isNull(collections.deletedAt))).returning()`. Add JSDoc.
- [x] 4.7 Implement `softDelete(id)` — `db.update(collections).set({ deletedAt: new Date() }).where(and(eq(collections.id, id), isNull(collections.deletedAt))).returning()`. Add JSDoc.
- [x] 4.8 Implement `addItem(collectionId, recipeId, sortOrder?)` — compute `sortOrder = sortOrder ?? (max existing + 1) ?? 0`, `db.insert(collectionItems).values({ collectionId, recipeId, sortOrder }).returning()`. Let the unique constraint throw on duplicate (service catches it). Add JSDoc.
- [x] 4.9 Implement `removeItem(collectionId, recipeId)` — `db.delete(collectionItems).where(and(eq(collectionItems.collectionId, collectionId), eq(collectionItems.recipeId, recipeId))).returning()`. Returns the deleted row or undefined. Add JSDoc.
- [x] 4.10 Implement `reorderItems(collectionId, itemIds)` — `db.transaction(async (tx) => { for (let i = 0; i < itemIds.length; i++) { await tx.update(collectionItems).set({ sortOrder: i }).where(and(eq(collectionItems.collectionId, collectionId), eq(collectionItems.id, itemIds[i]))); } })`. Add JSDoc.
- [x] 4.11 Implement `getItems(collectionId)` — `db.query.collectionItems.findMany({ where: and(eq(collectionItems.collectionId, collectionId)), orderBy: asc(collectionItems.sortOrder), with: { recipe: { with: { author: {...} } } } })` AND filter out soft-deleted recipes (either via a join condition or a post-filter). Add JSDoc.
- [x] 4.12 Implement `getCollectionsForRecipe(recipeId)` — find public collections containing a recipe (for RecipeDetailPage). Query `collectionItems` where `recipeId` matches, join `collections` where `visibility = 'public'` and `deletedAt IS NULL`. Add JSDoc.

## 5. API — Service Layer

- [x] 5.1 Create `apps/api/src/modules/collection/service.ts`. Add `// deno-lint-ignore-file no-explicit-any require-await` as line 1. Import `import * as model from './model.ts'`, `import { CollectionCreateSchema, ... } from '@brewform/shared/schemas'` (or `import type { CollectionCreate, CollectionUpdate }`), `import { createLogger } from '../../utils/logger/index.ts'`, `import * as recipeModel from '../recipe/model.ts'` (to fetch the recipe for visibility checks). Create `const logger = createLogger('collection-service')`.
- [x] 5.2 Implement `createCollection(userId, data)` — `logger.debug` entry, `model.create({ userId, ...data })`, fetch the created collection via `model.findById` for the rich shape, `logger.debug` exit, return `CollectionDetailOutput`. Add JSDoc.
- [x] 5.3 Implement `updateCollection(userId, collectionId, data)` — fetch, check `if (!collection) throw 'COLLECTION_NOT_FOUND'`, `if (collection.userId !== userId) throw 'FORBIDDEN'`, `model.update`, return rich shape. Entry/exit logs. Add JSDoc.
- [x] 5.4 Implement `deleteCollection(userId, collectionId)` — fetch, ownership check, `model.softDelete`. Entry/exit logs. Add JSDoc.
- [x] 5.5 Implement `getCollection(userId, collectionId)` — fetch, if not found throw `COLLECTION_NOT_FOUND`, if `visibility === 'private' || 'draft'` and `collection.userId !== userId` throw `FORBIDDEN`, return rich shape. Entry/exit logs. Add JSDoc.
- [x] 5.6 Implement `listMyCollections(userId, page, perPage, visibility?)` — `model.findByUserId`, return `{ collections, total }`. Entry/exit logs. Add JSDoc.
- [x] 5.7 Implement `listPublicCollections(userId, page, perPage)` — `model.findPublicByUserId`, return `{ collections, total }`. Entry/exit logs. Add JSDoc.
- [x] 5.8 Implement `addRecipeToCollection(userId, collectionId, recipeId, sortOrder?)` — fetch collection (ownership check, throw `COLLECTION_NOT_FOUND`/`FORBIDDEN`), fetch recipe via `recipeModel.findById`, `if (!recipe) throw 'RECIPE_NOT_FOUND'`, THEN check `if (recipe.visibility !== 'public' && recipe.authorId !== userId) throw 'FORBIDDEN'` (null-check BEFORE property access), `try { model.addItem } catch (unique violation) throw 'ALREADY_IN_COLLECTION'`. Entry/exit logs. Add JSDoc.
- [x] 5.9 Implement `removeRecipeFromCollection(userId, collectionId, recipeId)` — fetch collection (ownership check), `model.removeItem`. Entry/exit logs. Add JSDoc.
- [x] 5.10 Implement `reorderCollection(userId, collectionId, itemIds)` — fetch collection (ownership check), fetch items, `if (itemIds.length !== items.length || !all ids match) throw 'REORDER_MISMATCH'`, `model.reorderItems`. Entry/exit logs. Add JSDoc.

## 6. API — Route Layer

- [x] 6.1 Create `apps/api/src/modules/collection/index.ts`. Import `Hono`, `zValidator`, `describeRoute, resolver` from `hono-openapi`, `z` from `zod`, envelope helpers + schemas from `@brewform/shared/schemas`, `jsonRequestBody` from `../../utils/openapi/index.ts`, `authMiddleware, optionalAuthMiddleware` from `../../middleware/auth.ts`, `import * as service from './service.ts'`, response helpers `success, error, paginated, zodValidationHook` from `../../utils/response/index.ts`, `ErrorEnvelopeSchema, successEnvelope, paginatedEnvelope` from `@brewform/shared/schemas`, the collection output schemas. Create `const collection = new Hono<AppEnv>()`.
- [x] 6.2 Implement `GET /` — `describeRoute({ tags: ['Collections'], summary: 'List my collections', security: [{ bearerAuth: [] }], parameters: [page, perPage, visibility query], responses: { 200: resolver(paginatedEnvelope(CollectionListItemOutputSchema)), 401: resolver(ErrorEnvelopeSchema) } })`, `authMiddleware`, `zValidator('query', CollectionListFilterSchema)`, handler calls `service.listMyCollections`, returns `paginated(c, collections, { page, perPage, total, totalPages })`.
- [x] 6.3 Implement `GET /:id` — `describeRoute({ tags: ['Collections'], summary: 'Get a collection', parameters: [id path], responses: { 200: resolver(successEnvelope(CollectionDetailOutputSchema)), 403: resolver(ErrorEnvelopeSchema), 404: resolver(ErrorEnvelopeSchema) } })`, `optionalAuthMiddleware`, handler calls `service.getCollection(c.get('userId'), id)`, maps errors (COLLECTION_NOT_FOUND→404, FORBIDDEN→403).
- [x] 6.4 Implement `POST /` — `describeRoute({ tags: ['Collections'], summary: 'Create a collection', security: [{ bearerAuth: [] }], requestBody: jsonRequestBody(CollectionCreateSchema), responses: { 201: resolver(successEnvelope(CollectionDetailOutputSchema)), 400: resolver(ErrorEnvelopeSchema), 401: resolver(ErrorEnvelopeSchema) } })`, `authMiddleware`, `zValidator('json', CollectionCreateSchema, zodValidationHook)`, handler calls `service.createCollection(c.get('userId'), c.req.valid('json'))`, returns `success(c, result, 201)`.
- [x] 6.5 Implement `PATCH /:id` — `describeRoute({ ..., security: [{ bearerAuth: [] }], requestBody: jsonRequestBody(CollectionUpdateSchema), responses: { 200: resolver(successEnvelope(CollectionDetailOutputSchema)), 400, 401, 403, 404 } })`, `authMiddleware`, `zValidator('json', CollectionUpdateSchema, zodValidationHook)`, handler calls `service.updateCollection`, maps errors.
- [x] 6.6 Implement `DELETE /:id` — `describeRoute({ ..., security: [{ bearerAuth: [] }], parameters: [id], responses: { 200: resolver(successEnvelope(MessageResponseSchema)), 401, 403, 404 } })`, `authMiddleware`, handler calls `service.deleteCollection`, returns `success(c, { message: 'Collection deleted' })`.
- [x] 6.7 Implement `POST /:id/recipes` — `describeRoute({ ..., security: [{ bearerAuth: [] }], parameters: [id], requestBody: jsonRequestBody(CollectionAddRecipeSchema), responses: { 201: resolver(successEnvelope(MessageResponseSchema)), 400, 401, 403, 404, 409 } })`, `authMiddleware`, `zValidator('json', CollectionAddRecipeSchema, zodValidationHook)`, handler calls `service.addRecipeToCollection`, returns `success(c, { message: 'Recipe added' }, 201)`, maps ALREADY_IN_COLLECTION→409.
- [x] 6.8 Implement `DELETE /:id/recipes/:recipeId` — `describeRoute({ ..., security: [{ bearerAuth: [] }], parameters: [id, recipeId], responses: { 200: resolver(successEnvelope(MessageResponseSchema)), 401, 403, 404 } })`, `authMiddleware`, handler calls `service.removeRecipeFromCollection`, returns `success(c, { message: 'Recipe removed' })`.
- [x] 6.9 Implement `PATCH /:id/reorder` — `describeRoute({ ..., security: [{ bearerAuth: [] }], requestBody: jsonRequestBody(CollectionReorderSchema), responses: { 200: resolver(successEnvelope(MessageResponseSchema)), 400, 401, 403, 404 } })`, `authMiddleware`, `zValidator('json', CollectionReorderSchema, zodValidationHook)`, handler calls `service.reorderCollection`, returns `success(c, { message: 'Collection reordered' })`.
- [x] 6.10 `export default collection`.

## 7. API — Route Registration and OpenAPI

- [x] 7.1 Add `import collection from '../modules/collection/index.ts'` and `routes.route('/api/v1/collections', collection)` to `apps/api/src/routes/index.ts` (insert near the `recipe`/`comment` routes).
- [x] 7.2 Add `{ name: 'Collections', description: 'User-owned named collections of recipes' }` to the `tags` array in `apps/api/src/routes/openapi.ts`.
- [x] 7.3 Add `'/api/v1/collections'` to the `IN_SCOPE_BASE_PATHS` array in `apps/api/src/routes/openapi.coverage.test.ts`.
- [x] 7.4 Add a `GET /api/v1/users/:userId/collections` route (required for the UserProfilePage collections tab) to the user or collection module. It calls `collectionService.listPublicCollections` for non-self viewers, or `collectionService.listMyCollections` when `userId === requestingUserId`. Uses `optionalAuthMiddleware` (the requesting user may be unauthenticated). Full `describeRoute` metadata with `tags: ['Collections']` (or `['Users']` if mounted on the user router), `parameters: [userId path, page/perPage query]`, `responses: { 200: resolver(paginatedEnvelope(CollectionListItemOutputSchema)), 404: resolver(ErrorEnvelopeSchema) }`. Register in `routes/index.ts` if not part of the collection sub-router.

## 8. API — Tests

- [x] 8.1 Create `apps/api/src/modules/collection/model_test.ts` — DB integration tests for each model function: `findById` (found, not found, excludes soft-deleted), `findByUserId` (paginated, recipeCount correct), `findPublicByUserId` (only public), `create`, `update`, `softDelete`, `addItem` (with auto sortOrder, duplicate throws), `removeItem`, `reorderItems` (sortOrder = index), `getItems` (excludes soft-deleted recipes, ordered by sortOrder), `getCollectionsForRecipe`. Use `beforeEach`/`afterEach` to create/delete test users + collections + recipes. `sanitizeOps: false, sanitizeResources: false`.
- [x] 8.2 Create `apps/api/src/modules/collection/service_test.ts` — DB integration tests for permission checks: `createCollection` (returns rich shape), `updateCollection` (owner succeeds, non-owner throws FORBIDDEN, not-found throws COLLECTION_NOT_FOUND), `getCollection` (public visible to all, private visible to owner only, private throws FORBIDDEN for non-owner), `addRecipeToCollection` (public recipe by non-owner succeeds, private recipe by non-owner throws FORBIDDEN, duplicate throws ALREADY_IN_COLLECTION), `reorderCollection` (correct order succeeds, wrong count throws REORDER_MISMATCH). Verify entry/exit debug logs.
- [x] 8.3 Create `apps/api/src/modules/collection/index_test.ts` — route integration tests using `createTestApp(userId)` pattern (deps-swappable auth if using the `deps` proxy, or direct stub auth). Test: `GET /` returns paginated own collections, `GET /:id` returns 403 for private by non-owner, `POST /` returns 201, `PATCH /:id` returns 200 for owner / 403 for non-owner, `DELETE /:id` returns 200, `POST /:id/recipes` returns 201 / 409 for duplicate, `DELETE /:id/recipes/:recipeId` returns 200, `PATCH /:id/reorder` returns 200 / 400 for mismatch. Assert status + `body.success` + `body.error.code`.
- [x] 8.4 Run `make test-specific filter=openapi.coverage.test.ts` and fix any failures (all 8 routes documented, tagged, no orphans).

## 9. Frontend — API Client

- [x] 9.1 Add `collectionApi` object to `apps/web/src/api/index.ts` with `list`, `get`, `create`, `update`, `delete`, `addRecipe`, `removeRecipe`, `reorder`, `listByUser` methods. Import the shared types (`CollectionListItemOutput`, `CollectionDetailOutput`, `CollectionCreate`, `CollectionUpdate`) from `@brewform/shared/schemas`. Add JSDoc on the object.

## 10. Frontend — Pages and Router

- [x] 10.1 Create `apps/web/src/pages/collections/CollectionListPage.tsx` — react-router `loader` calls `collectionApi.list()`, component uses `useLoaderData()`, `useAuth()`, `useTranslation()`, renders a grid of `<CollectionCard>` + a "Create Collection" button (opens a modal or navigates to a create form). `const log = createLogger('CollectionListPage')` + mount/unmount `useEffect`. Add JSDoc.
- [x] 10.2 Create `apps/web/src/pages/collections/CollectionDetailPage.tsx` — `loader` calls `collectionApi.get(id)`, maps 404 `ApiError` to `throw new Response('Not Found', { status: 404 })`. Component renders header (name, description, visibility badge, owner link), `<CollectionRecipeList>`, edit/delete buttons for owner. `const log = createLogger('CollectionDetailPage')` + mount/unmount `useEffect`. Add JSDoc.
- [x] 10.3 Add two routes to `apps/web/src/router.tsx`: `{ path: 'collections', element: <RequireAuth><CollectionListPage /></RequireAuth>, loader: collectionListLoader, errorElement: <RootErrorBoundary /> }` (eager import + separate loader export, matching `RecipeListPage` Pattern A) and `{ path: 'collections/:id', element: <CollectionDetailPage />, loader: collectionDetailLoader, errorElement: <RootErrorBoundary /> }` (eager import + separate loader export, matching `RecipeDetailPage` at `router.tsx:98-103`). Export `collectionListLoader` from `CollectionListPage.tsx` and `collectionDetailLoader` from `CollectionDetailPage.tsx`. If code-splitting is desired later, switch to `lazy: async () => { const { CollectionDetailPage, loader } = await import('./...'); return { Component: CollectionDetailPage, loader } }` — but start with eager imports to match the established recipe-page pattern.
- [x] 10.4 (Optional) Add `Disallow: /collections` to the robots.txt generation in `apps/web/vite.config.ts` (collections are auth-only, should not be indexed).

## 11. Frontend — Components

- [x] 11.1 Create `apps/web/src/components/collections/CollectionCard.tsx` — `<Link to={\`/collections/${c.id}\`} className='card hover:shadow-lg transition-shadow'>` showing name, visibility badge (🔒/🌐), recipeCount. Tailwind + `var(--text-*)` CSS vars. `const log = createLogger('CollectionCard')`. Add JSDoc.
- [x] 11.2 Create `apps/web/src/components/collections/AddToCollectionModal.tsx` — hand-rolled modal (matching `BanDialog.tsx`: `fixed inset-0 z-50` overlay + `card` content, controlled by `open`/`onClose` props). Fetches user's collections via `collectionApi.list()` in `useEffect`, shows checkmark for collections containing the current `recipeId`, supports inline create (name + visibility dropdown). Add/remove calls `collectionApi.addRecipe`/`removeRecipe`. `const log = createLogger('AddToCollectionModal')`. Add JSDoc.
- [x] 11.3 Create `apps/web/src/components/collections/CollectionRecipeList.tsx` — renders items ordered by `sortOrder`. Each item: recipe title as `<Link>`, up/down arrow buttons (optimistic reorder via `useFetcher` PATCH to `/collections/:id/reorder` action), remove button (calls `collectionApi.removeRecipe`). `const log = createLogger('CollectionRecipeList')`. Add JSDoc.
- [x] 11.4 Create `apps/web/src/components/collections/AddToCollectionButton.tsx` — thin wrapper managing `open` state, renders a button + `<AddToCollectionModal>`. Add JSDoc.
- [x] 11.5 Add `{isAuthenticated && <AddToCollectionButton recipeId={recipe.id} />}` to `RecipeDetailPage.tsx` action-buttons row (around line 192-235).
- [x] 11.6 (Optional, US-9) Add a "Collections containing this recipe" section to `RecipeDetailPage.tsx` sidebar. Fetch via a new `collectionApi.listByRecipe(recipeId)` method (calls `GET /api/v1/recipes/:id/collections` or a query param on the collection list). Render as a small list of `<CollectionCard>` or collection-name links. This requires a corresponding `getCollectionsForRecipe` model function (task 4.12) surfaced through a route — either a `GET /api/v1/recipes/:id/collections` route (public collections only) or reuse `GET /collections?recipeId=...`. Add the route + `describeRoute` if a new endpoint is created. If deferred, document the deferral in the PR description.

## 12. Frontend — UserProfilePage Collections Tab

- [x] 12.1 Modify `apps/web/src/pages/users/UserProfilePage.tsx`: add `'collections'` to the `Tab` union and `ALLOWED_TABS`, add `{ key: 'collections', label: t('user.collections') }` to the tabs array, add a conditional fetch branch in the `loader` (`else if (tab === 'collections') { collectionsData = await collectionApi.listByUser(profile.id) }`), add a conditional render block (grid of `<CollectionCard>` after the recipes block, with empty-state message).
- [x] 12.2 Add `collectionsData` to the `ProfileLoaderData` type and the loader return.

## 13. Frontend — i18n

- [x] 13.1 Add all `collection.*` and `user.collections` / `user.noCollections` keys to `packages/shared/src/i18n/en.json`.
- [x] 13.2 Add the same keys to `packages/shared/src/i18n/tr.json` with valid Turkish translations.

## 14. Seed

- [x] 14.1 Add `seedCollections(tx, createdUsers, createdRecipes)` helper to `packages/db/src/seed.ts` — creates 1–2 public collections per seeded user, adds 2–3 recipes each, uses `onConflictDoNothing({ target: [collectionItems.collectionId, collectionItems.recipeId] })`, select-and-reuse for FK IDs. Add JSDoc.
- [x] 14.2 Call `seedCollections(tx, createdUsers, createdRecipes)` from `main()` inside the `db.transaction` block after `seedRecipes`.
- [x] 14.3 Run `make db-seed` twice and verify idempotency (no errors, row counts unchanged on second run).

## 15. Format, Lint, Type-Check, Tests

- [x] 15.1 Run `make fmt` — auto-format all changed files (lineWidth 100, indentWidth 2, singleQuote, semiColons)
- [x] 15.2 Run `make lint` — fix any lint errors
- [x] 15.3 Run `make check` — fix any TypeScript errors
- [x] 15.4 Run `make test` — ensure all tests pass (new + existing)
- [x] 15.5 Verify test coverage ≥80% for all new code paths
- [x] 15.6 Run `make test-specific filter=openapi.coverage.test.ts` — confirm all 8 collection routes are documented, tagged, and no orphan tags exist