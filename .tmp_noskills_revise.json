{"revise":{"reversibility":"Yes — making the field mandatory is an irreversible decision for existing API consumers. Old payloads without preparationNotes will be rejected. However, the user confirms the app will be re-deployed from scratch, so this is acceptable.","user_impact":"This is a breaking change. The preparationNotes field is now mandatory on recipe versions. Old API payloads without preparationNotes will fail validation. All 6 seed recipes must include preparationNotes. Existing database rows would fail if not migrated with default values — but the user confirms a fresh deploy."}}
